{
    "name": "@wermote/api-gateway",
    "version": "1.0.0",
    "scripts": {
        "deploy": "cd infra; pulumi stack select dev; pulumi up --skip-preview",
        "deploy:ci": "cd infra; pulumi stack select $ENV; pulumi up --skip-preview"
    },
    "dependencies": {
        "@wermote/architect": "^1.0.0"
    }
}
